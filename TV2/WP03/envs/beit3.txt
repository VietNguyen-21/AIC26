# Windows inference dependencies for the isolated BEiT-3 worker.
# Install a CUDA build of torch/torchvision for the target GPU before this file.
# Clone and pin UniLM separately; WP03 imports it through WP03_BEIT3_SOURCE_DIR.
# Do NOT run `pip install -r third_party/unilm/beit3/requirements.txt` for WP03
# inference. That training/evaluation requirements file pulls legacy DeepSpeed,
# which is unnecessary here and does not build reliably on modern Windows Python.
timm==0.4.12
torchscale==0.2.0
transformers>=4.45,<5
sentencepiece>=0.2,<1
pillow>=10
