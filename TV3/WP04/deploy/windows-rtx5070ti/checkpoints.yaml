# This file is the auditable checkpoint manifest. setup.ps1 records downloaded revisions
# and SHA256 values into checkpoints.lock.json on Việt's machine.
asr:
  provider: huggingface
  repository: khanhld/chunkformer-ctc-large-vie
  revision: 311fc03558a895dc2b32957f2fb4236c7fb1455b
  license: CC-BY-NC-4.0
  fetch: snapshot_download
object:
  provider: pypi
  package: rfdetr
  model: RFDETRMedium
  checkpoint: package-managed
ocr:
  provider: local-team-artifact
  runtime: DeepSolo + PARSeq-VN
  required_environment_variables:
    - DEEPSOLO_HOME
    - DEEPSOLO_CHECKPOINT
    - PARSEQ_CHECKPOINT
  reason: The team requested local implementation only; no DeepSolo source or checkpoint is copied/downloaded by this repository.
